syntax = "proto3";
option java_multiple_files = true;
//package data.proto;

message GameNotification {
    string text = 1;
}

message None {

}

service Futsal {
    rpc notify(GameNotification) returns (None) {};
}